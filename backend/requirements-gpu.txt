# GPU-only extra dependencies (installed on top of requirements.txt in the
# on-demand GPU image, Dockerfile.gpu). NOT installed in the slim CPU webapp.
#
# CUDA-Q ships on PyPI as a wheel-stub sdist that downloads the real prebuilt
# wheel from NVIDIA's index at install time, so:
#   - pip must be >= 24.0 (Dockerfile.gpu upgrades pip before installing), and
#   - the build host needs network access to pypi.nvidia.com.
# Requires Python >= 3.11 and Linux x86_64. No GPU is needed to BUILD; the GPU
# is only used at runtime.
cudaq==0.16.0
